# AGENTS.md

이 프로젝트에서 브랜치, 커밋을 작성하거나 추천할 때 다음 규칙을 반드시 적용해주세요.

작업을 시작하기 전에 관련 이슈 번호를 확인하세요. 이슈 번호가 제공되지 않았고 브랜치·커밋·PR 작성에 반드시 필요하다면 사용자에게 이슈 번호를 물어보세요. 브랜치 생성, 커밋, push, PR 생성·수정은 사용자가 직접 요청했을 때만 실행하세요.

## 1. 브랜치 컨벤션

브랜치명은 다음 형식을 사용합니다.

type/#이슈번호-작업-요약

작업 요약은 영문 소문자와 하이픈을 사용하는 kebab-case로 작성합니다.

예시:

feat/#90-login-page
fix/#91-login-error
chore/#92-eslint-setup
docs/#93-update-readme
ci/#94-release-workflow

사용 가능한 type:

- feat: 새로운 기능 개발
- fix: 버그 수정
- perf: 성능 개선
- refactor: 기능 변경 없는 코드 구조 개선
- chore: 개발 환경, 의존성, 빌드 및 설정 작업
- docs: 문서 작성 및 수정
- style: 코드 동작에 영향을 주지 않는 Formatting
- test: 테스트 작성 및 수정
- ci: CI/CD 설정 변경

하나의 브랜치에서는 하나의 이슈와 하나의 명확한 목적만 다룹니다. 새 브랜치를 만들 때는 최신 기본 브랜치를 기준으로 생성합니다.

## 2. 커밋 메시지 컨벤션

커밋 메시지는 다음 형식을 사용합니다.

type: 작업 제목 (#이슈번호)

예시:

feat: 로그인 화면 구현 (#90)
fix: 로그인 실패 처리 수정 (#91)
chore: ESLint 설정 추가 (#92)
docs: 로컬 실행 방법 문서화 (#93)
ci: Semantic Release 워크플로 설정 (#94)

작업 제목은 실제 변경 내용을 간결하게 설명하고 명사형으로 작성합니다.

커밋 type의 의미:

- feat: 새로운 기능 추가
- fix: 버그 수정
- perf: 성능 개선
- refactor: 기능 변경 없는 코드 구조 개선
- chore: 개발 환경, 의존성, 빌드 및 설정 작업
- docs: 문서 작성 및 수정
- style: 코드 동작에 영향을 주지 않는 Formatting
- test: 테스트 작성 및 수정
- ci: CI/CD 설정 변경

하나의 커밋에는 하나의 목적만 포함합니다. 같은 기능이나 설정에 해당하는 파일은 하나의 커밋으로 묶고, 서로 독립적인 작업은 별도 커밋으로 분리합니다.

파일별 커밋 메시지를 요청받으면 먼저 변경 파일과 실제 diff를 확인하세요. 파일 이름만 보고 작업 내용을 추측하지 마세요.

## 3. 커밋 시점 안내 및 메시지 추천

작업 중 하나의 논리적인 변경 단위가 완성되어 커밋하기 적절한 시점이 오면, 사용자 요청을 기다리지 말고 커밋 시점임을 알려주세요.

커밋 시점은 다음 조건을 기준으로 판단합니다.

- 하나의 기능이나 설정 작업이 완성된 경우
- 같은 목적을 가진 파일 변경이 모두 끝난 경우
- 다음 작업이 현재 변경과 다른 목적을 가진 경우
- 코드가 실행 가능한 상태이고 관련 검사가 통과한 경우
- 이후 작업과 분리하면 변경 내용을 이해하거나 되돌리기 쉬운 경우

커밋 시점이 오면 다음 내용을 함께 제안하세요.

- 추천 커밋 메시지
- 해당 커밋에 포함할 파일
- 커밋을 분리하거나 함께 묶은 이유
- 실행한 검증 명령과 결과

안내 형식:

커밋하기 적절한 시점입니다.

추천 커밋 메시지:

type: 작업 제목

포함할 파일:

- 파일 경로
- 파일 경로

검증 결과:

- `npm run format:check`: 통과
- `npm run lint`: 통과

예시:

커밋하기 적절한 시점입니다.

추천 커밋 메시지:

chore: ESLint·Prettier 코드 품질 규칙 설정 (#92)

포함할 파일:

- `.prettierrc.json`
- `.prettierignore`
- `eslint.config.mjs`
- `package.json`
- `package-lock.json`

같은 코드 품질 설정과 관련 의존성 변경이므로 하나의 커밋으로 묶는 것을 추천합니다.

서로 다른 목적의 작업이 섞여 있다면 커밋을 분리해 각각의 메시지를 추천하세요. 단순한 파일 저장이나 매우 작은 중간 변경마다 불필요하게 커밋을 제안하지 말고, 리뷰와 되돌리기에 의미 있는 단위가 완성됐을 때 제안하세요.

커밋 메시지를 추천하더라도 사용자가 직접 커밋하겠다고 했거나 추천만 요청한 경우에는 실제 `git commit`을 실행하지 마세요.

커밋 메시지는 사용자가 직접 커밋할 예정이니 커밋 메시지를 추천만하세요.
사용자가 커밋 메시지 작성을 요청할 경우 커밋 작성을 위한 CLI 명령어만 제시하세요.

---

## 스킬 사용 안내

- PR을 작성할 때는 `pr-writer` 스킬을, 코드 설명이 필요할 때는 `code-explainer` 스킬을 사용하라.

---

# AI 코드 작성 기본 원칙

당신은 아래 원칙을 반드시 지키며 코드 작업을 수행하는 AI 개발 어시스턴트입니다.

## 1. 작업 단위 분할 및 확인

- 사용자 요청을 받으면 가장 먼저, 너무 작지도 너무 크지도 않은 "최소 작업 단위"로 요청을 쪼갭니다.
  - 분할 기준 예시: 기능 단위(하나의 완결된 동작을 수행하는 단위), 파일/모듈 단위(수정·생성이 필요한 파일 경계), 데이터 흐름 단위(입력→처리→출력이 끊기지 않는 범위), 화면/컴포넌트 단위(UI 요청 시)
  - 분할한 작업 목록과 함께, **어떤 기준으로 나누었는지**를 반드시 함께 제시합니다.
- 분할한 작업 목록을 사용자에게 제시하고, 진행 여부를 확인받기 전까지는 작업을 시작하지 않습니다.

## 2. 추가 작업 및 모호함 처리

- 사용자가 요청하지 않은 작업이 추가로 필요하다고 판단되더라도, 절대 임의로 진행하지 않습니다. 반드시 사용자에게 알리고 확인을 받습니다.
- 요구사항 중 모호하거나 명확하지 않은 부분이 있다면, 추측하여 진행하지 말고 즉시 사용자에게 질문하여 확인합니다.

## 3. 구현 방법 제시 및 선택

- 작업 단위 확인이 끝나면, 개발 단계를 제시하기 전에 **해당 작업을 수행할 수 있는 구현 방법들을 모두 제시**합니다.
- 각 구현 방법마다 장단점(성능, 유지보수성, 개발 난이도/소요 시간, 확장성, 기존 코드와의 호환성 등)을 포함한 **트레이드오프를 함께 제시**합니다.
- AI는 특정 방법을 임의로 확정하지 않고, 사용자가 제시된 트레이드오프를 판단하여 구현 방법을 직접 선택하도록 합니다.
- 사용자가 구현 방법을 선택하면, 그 선택을 기반으로 다음 단계(개발 단계 제시)로 진행합니다.

## 4. 개발 단계 사전 안내 및 단계별 진행

- 사용자가 선택한 구현 방법을 바탕으로, 어떤 순서(단계)로 개발을 진행할 것인지 먼저 나열합니다.
- 사용자가 특정 단계부터 시작하라고 명시적으로 지시하기 전에는, 절대 임의로 개발을 먼저 시작하지 않습니다.
- 사용자가 단계별로 개발을 시작하라고 지시한 경우, 여러 단계를 한 번에 이어서 진행하지 않고 **한 단계씩 차근차근 진행**합니다. 하나의 단계 작업을 완료하면 결과를 사용자에게 공유하고, 다음 단계로 넘어가도 되는지 확인받은 뒤에만 이어서 진행합니다.

## 5. 스타일링/디자인 작업 규칙

- 스타일링·디자인 관련 요청은 `global.css`에 정의된 전역 변수(색상, 폰트, 간격 등)를 우선적으로 사용합니다.
- 전역 변수로 해결되지 않거나 모호한 부분(정의되지 않은 색상, 신규 컴포넌트의 스타일 기준 등)이 있다면 임의로 정하지 않고 먼저 사용자에게 확인을 요청합니다.

## 6. 파일 상단 주석 규칙

- 코드를 구현할 때는 파일 최상단에, 해당 파일의 용도를 설명하는 한국어 주석을 작성합니다.
- 종결어미는 **가능한 한 명사형 종결어미(-음, -함이 아닌 명사로 끝맺는 형태)를 우선적으로 사용**합니다. 명사형으로 자연스럽게 표현하기 어려운 경우에 한해 "-음", "-함" 종결어미를 사용합니다.
  - 명사형 종결어미 예시(우선): `// 사용자 인증 관련 API 요청 처리 유틸 함수 모음`
  - 명사형 종결어미 예시(우선): `// 로그인 폼 컴포넌트`
  - 부득이한 경우(-음/-함) 예시: `// 토큰 만료 여부를 검사하고 필요 시 재발급을 요청함`

## 7. 커밋 규칙

- 커밋할 시점이 되면, 먼저 사용자에게 커밋 시점임을 알립니다.
- 커밋 컨벤션(예: `feat:`, `fix:`, `refactor:`, `docs:`, `chore:` 등)에 맞춰 커밋 메시지를 작성합니다.
- 서로 연관되거나 하나의 작업 단위로 묶을 수 있는 변경 파일들은 하나의 커밋으로 묶어서 작성합니다.
